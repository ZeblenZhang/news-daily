# 手把手推送代码到 GitHub（零基础版）

> 目标：把本机已经准备好的代码（含定时任务文件）推送到你的 GitHub 仓库。
> 全程约 5 分钟，分两大步：①生成 token ②终端推送。

---

## 第一步：重新生成一个 Token（2 分钟）

1. 用浏览器打开这个网址（登录你的 GitHub）：
   ```
   https://github.com/settings/tokens
   ```

2. 页面中间偏上，点蓝色按钮 **「Generate new token」**，再选 **「Generate new token (classic)」**。

3. 填写表单：
   - **Note**（备注）：随便填，比如 `push`
   - **Expiration**（有效期）：选 `90 days`（90天）即可
   - **Select scopes**（权限）：找到 **`repo`** 这一行，**勾选它前面的大方框**（整行勾上，下面所有子项会自动勾上）

4. 滑到页面最底部，点绿色按钮 **「Generate token」**。

5. 页面上会出现一串 `ghp_` 开头的字符，**点复制按钮复制它**（注意：这串字符只显示这一次，关掉页面就看不到了）。

6. **先把它粘贴到记事本里保存好**，马上要用。

---

## 第二步：在电脑上打开终端并推送（3 分钟）

### 2.1 打开终端
- 按键盘上的 `Win` 键 + `R` 键（同时按）
- 弹出的小窗口里输入 `cmd`，点「确定」
- 会弹出一个黑色背景的命令行窗口

### 2.2 进入项目目录
在黑色窗口里，用鼠标右键粘贴下面这行，然后按回车：
```
cd C:\Users\admin\WorkBuddy\2026-09-01-09-46-41\news-daily
```

### 2.3 设置远程地址
粘贴下面这行，回车：
```
git remote add origin https://github.com/ZeblenZhang/news-daily.git
```
> 如果出现 `remote origin already exists` 的提示，不用管，继续下一步。

### 2.4 推送代码（核心命令）
粘贴下面这行，回车：
```
git push --force origin main
```

### 2.5 登录验证（重点！）
命令执行后，**可能会弹出登录窗口**，或者命令行里让你输入用户名和密码：

- **用户名** 填：`ZeblenZhang`
- **密码** 填：**第一步生成的那串 `ghp_` 开头的 token**（不是你的 GitHub 登录密码！）

> 如果命令行里让你输密码，输入时屏幕上是**看不到字符**的（这是正常的安全设计），直接粘贴 token 然后回车即可。

### 2.6 看到成功提示
如果出现类似下面这样的文字，就是成功了：
```
main -> main
```
或 `* [new branch]` 之类的字样。

---

## 第三步：启用自动部署（网页，1 分钟）

1. 打开：`https://github.com/ZeblenZhang/news-daily/settings/pages`
2. 找到 **Build and deployment**，**Source** 下拉框选 **GitHub Actions**
3. 完成

之后每天北京时间 09:30 自动更新，访问：
```
https://ZeblenZhang.github.io/news-daily/
```

---

## 如果卡住了

把**命令行窗口里显示的报错文字**（截图或复制文字）发给我，我帮你判断下一步。

---

# 日常更新指南（改完代码后怎么同步到 GitHub）

> 场景：以后每次我在本地改完代码（比如调整布局、改分类规则、加新闻源），
> 你只需要打开终端执行下面几条命令，改动就会同步到 GitHub，网页自动更新。

## 每次更新只需要 4 步

打开终端（`Win` + `R`，输入 `cmd`，回车），然后依次粘贴执行下面 4 行命令。

### ① 进入项目目录
```
cd C:\Users\admin\WorkBuddy\2026-09-01-09-46-41\news-daily
```

### ② 把改动加入提交清单
```
git add .
```

### ③ 提交改动（引号里是本次改动的说明，可自己改）
```
git commit -m "更新页面布局与行业趋势子分类"
```

### ④ 推送到 GitHub
```
git push origin main
```

> 第④步如果弹出登录窗口，用户名填 `ZeblenZhang`，密码填 token（`ghp_` 开头那串）。
> 如果之前已经配置过代理，遇到 `Could not connect` 就先执行：
> `git config --global http.proxy http://127.0.0.1:7897`

## 推送成功后

- 等 1～2 分钟，GitHub Actions 会自动重新构建并部署。
- 打开网页 `https://ZeblenZhang.github.io/news-daily/` 就能看到最新效果。
- 如果没生效，按 `Ctrl` + `F5` 强制刷新（清缓存）。

## 判断是否成功

执行完第④步后，如果看到类似下面这些字，就是成功了：
```
main -> main
```
或 `Writing objects: 100%` 等字样；如果报错，把报错文字发给我即可。
